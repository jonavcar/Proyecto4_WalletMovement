server:
  port: 8086

spring:
    application:
        name: wallet-microservice
    data:
        mongodb:
            database: wallet
            host: localhost
            port: 27017
            